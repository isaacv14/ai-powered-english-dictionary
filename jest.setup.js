/**
 * Jest Setup File
 * 
 * Runs before all tests. Use this to configure test environment.
 */

// Optional: Add custom matchers, mocks, etc. here
// Example: jest.mock('next/router')

// Suppress console output during tests (optional)
// beforeAll(() => {
//   jest.spyOn(console, 'log').mockImplementation(() => {});
// });
